server:
  port: 7070
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fhantoing/spring-api-microservices
          search-paths: configuracion-data
        default-label: main
  application:
    name: configuracion-service